#include "exec.h"

#include <stdio.h>
#include <stdlib.h>

void vm_run(Program *p) {
    /* placeholder: fetch/decode/execute loop comes in V2 */
    (void)p;
    fprintf(stderr, "error: vm_run not implemented yet\n");
    exit(1);
}
